interface:
  display_name: "Audit (RCP)"
  short_description: "Full-project audit: bugs, security, architecture, debt"
  default_prompt: "Use $audit-rcp to audit this repository and report only verified, prioritized findings without changing code."
policy:
  allow_implicit_invocation: false
